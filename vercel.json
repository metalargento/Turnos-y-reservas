{
  "buildCommand": "npm --prefix frontend run build",
  "installCommand": "npm --prefix frontend install",
  "routes": [
    {
      "src": "^/(?!assets/).*",
      "dest": "/index.html"
    }
  ]
}
